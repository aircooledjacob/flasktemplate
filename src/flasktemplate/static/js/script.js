"use strict";

// window.setInterval(() => {
//     document.getElementsByTagName("header")[0].style.backgroundColor = `rgb(${Math.round(Math.random()*255)}, ${Math.round(Math.random()*255)}, ${Math.round(Math.random()*255)})`
// }, 1000)
    